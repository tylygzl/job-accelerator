# 求职加速器 · 产品架构清单

> 按「用户最远 → 代码最近」排序，一层层核对。打钩的已经做了，没打钩的还没做。

---

## 第一层：用户与需求

- [ ] **目标用户是谁？** 你自己（求职中的大学生），以后扩展到其他求职者
- [ ] **解决什么问题？** 投简历前不知道自己跟岗位匹不匹配，面试不知道会被问什么
- [ ] **用户一句话描述这个产品？** 贴 JD + 简历 → 出三栏评估 + 匹配度分数 + 面试预测题
- [ ] **竞品是谁？** 没有直接竞品（传统简历评估靠人工），间接竞品：BOSS 直聘自动匹配、猎头人工评估
- [ ] **差异化在哪？** 不从模糊数据靠大模型猜。JD 一键拆解对齐简历，三栏逐条评估，公司按行业尺寸对比，面试题针对这家公司出而非通用题库。

---

## 第二层：产品形态

- [ ] **用户怎么用？**
  - MVP 阶段：终端交互（`input()` 贴 JD → 贴简历 → 等结果）
  - 产品化阶段：网页界面（Streamlit 一行部署，或 uni-app 小程序）
- [ ] **输入什么？** 岗位描述文本 + 简历文本（现阶段硬编码，下一步改交互）
- [ ] **输出什么？** 三栏评估表 + 匹配度分数 + 面试预测题清单
- [ ] **结果怎么存？** 现阶段不存，产品化加历史记录（StoreBackend）

---

## 第三层：功能清单

- [x] **JD 拆解** — 拆成硬性要求 / 加分项 / 软技能
- [x] **三栏评估** — 🟢能说硬 / 🟡学两天能说 / 🔴暂时说不了
- [x] **匹配度打分** — 硬性 60% + 加分 25% + 软技能 15%
- [x] **面试预测** — 从缺口和强项抽题，标来源和概率
- [ ] **交互输入** — 用户不碰代码，直接贴 JD 和简历
- [ ] **输出格式** — 不只是终端打印，考虑导出 MD/PDF/一键复制
- [ ] **错误处理** — JD 太短、简历太短、模型返回格式不对，怎么提示用户

---

## 第四层：技术架构

- [x] **选型** — Deep Agents（LangGraph）+ DeepSeek V4
- [x] **核心架构** — 1 个主 Agent + 3 个子 Agent（jd-analyzer / resume-matcher / interview-predictor）
- [x] **模型配置** — `ChatOpenAI(model="deepseek-chat", base_url="https://api.deepseek.com/v1")`
- [x] **环境管理** — `uv` + `pyproject.toml` + `uv.lock`，依赖版本锁死
- [x] **密钥管理** — `.env` 存 API Key，不写死代码里
- [ ] **权限控制** — 现在是裸跑，生产环境加 `FilesystemPermission`
- [ ] **错误处理** — 模型超时/返回格式不对，Agent 有容错机制
- [ ] **日志/追踪** — LangSmith Tracing（现阶段因 bug 关着）

---

## 第五层：数据流

```
用户贴 JD + 简历
     ↓
main.py: agent.invoke({"messages": ["请拆解以下岗位..."]})
     ↓
主 Agent（求职顾问）读自己的 system_prompt → 决定委派
     ↓
┌─ task(name="jd-analyzer")  → JD 拆解专家 → 输出三组清单
├─ task(name="resume-matcher") → 人岗匹配师 → 输出三栏 + 分数 + 总评
└─ task(name="interview-predictor") → 面试出题官 → 输出 5+ 道预测题
     ↓
主 Agent 汇总三个结果 → 输出完整评估报告
     ↓
终端打印（现阶段）/ 网页展示（产品化阶段）
```

- [x] **数据流走通了** ✅
- [ ] **数据持久化** — 结果存入 StoreBackend，用户可以翻历史
- [ ] **多用户隔离** — `thread_id` 区分不同用户
- [x] **输出格式约定** — 主 Agent 汇总规则已写死 system_prompt（三部分完整输出，不删减改写）

---

## 第六层：交付与复现

- [ ] **`.env.example`** — 别人拉仓库后知道要填哪些环境变量
- [ ] **`README.md`** — 一句话介绍 + 安装步骤 + 怎么跑
- [ ] **Git 仓库** — `git init` + 推到 GitHub
- [ ] **依赖可复现** — `pyproject.toml` + `uv.lock` 已锁死 ✅
- [ ] **一键跑通** — 别人 `uv run python main.py` 就能出结果
- [ ] **测试用例** — 准备 2~3 份不同方向的 JD + 简历，验证输出质量

---

## 第七层：产品化路线（MVP 之后）

- [ ] 终端交互 → Streamlit 网页界面
- [ ] 单用户 → 多用户（thread_id 隔离 + StoreBackend 存历史）
- [ ] 硬编码 JD → 支持粘贴 + 上传文件
- [ ] 终端输出 → 导出 MD/PDF
- [ ] 没有前端 → uni-app 小程序（调后端 API）
- [ ] 单次评估 → 多次评估对比（同一份简历投 5 个岗，横向对比）

---

## 当前状态总览

| 层 | 完成度 | 缺什么 |
|---|---|---|
| 用户与需求 | 60% | 用户画像没写 |
| 产品形态 | 30% | 只有 CLI，没有交互 |
| 功能清单 | 60% | 交互输入 / 输出格式化 / 错误处理 |
| 技术架构 | 70% | 权限 / 错误处理 / Tracing |
| 数据流 | 50% | 跑通但没持久化、没多用户 |
| 交付复现 | 20% | README / .env.example / Git |
| 产品化 | 0% | 网页 / 小程序 / 导出 |

> 下一步选定一层开始补，不等不拖。
