# 求职加速器主窗口交接

> 更新时间：2026-08-05。本文件不记录 API Key、访问令牌、真实简历或完整聊天正文。

## 0. 主窗口 V3 接管说明

- 主窗口 V2 已停止承担项目主控职责；其后半段混入浏览器环境排查和无关系统问题，不应把完整对话历史当作项目事实来源。
- V3 只以当前代码、自动测试、`docs/project_memory.md`、本文件和 `docs/stage_b_plan.md` 为准；专项问题再按记忆路由读取对应专项记忆。
- 阶段 B 按 Beta 口径收口，阶段 C 尚未正式启动。V3 的第一项工作是固定审核和提交准备，不新增阶段 C 功能。
- 当前禁止自动提交、打包、部署或回滚已有修改。真实 BOSS 操作继续由用户人工执行，插件不得自动发送消息。

## 1. 当前产品方向和不可违反的约束

- 产品是同一个 Chrome 插件内的两种工作模式：岗位页海投和消息页 HR 回复助手，不拆成两个互不知状态的插件。
- 用户先在 BOSS 页面设置城市、薪资、经验等筛选条件；插件负责读取 JD、匹配简历、处理达标岗位，不替代 BOSS 的筛选器。
- 海投主流程优先保证快速、可恢复；快速投递完全不依赖 LLM，智能能力失败或超时必须兜底，不能卡住海投。
- 同一个 BOSS 标签页同一时间只能执行一种页面操作。任务优先级固定为：用户手动操作 > HR 回复处理 > 海投扫描。
- 海投运行时，HR 回复助手只能发现消息、去重、加入待回复队列和提醒，不能抢页面、跳转聊天页或填输入框。
- 只有用户点击“处理回复”后，插件才可以暂停海投、定位会话、调用 `/chat/reply` 并填入草稿。
- HR 回复助手只填草稿，绝不自动发送；不自动同意附件简历，不自动交换电话或微信，不代替用户回答高风险问题。
- 不绕过 BOSS 登录、验证码、MFA、风控或反爬机制；不保存 BOSS 密码、Cookie、完整聊天记录。
- 仓库不得包含真实简历、聊天正文、API Key、朋友包访问令牌或其他隐私数据。
- 当前 HTTP IP 云端入口只允许小范围测试；扩大分发前必须完成 HTTPS 和凭据轮换。

## 2. 阶段 A、阶段 B 已完成到哪里

### 阶段 A

- 已冻结在提交 `f859994`：`feat: complete stage A job accelerator workflow`。
- 已完成岗位扫描、简历匹配、达标岗位自动沟通、自动选择“留在此页”、继续下一条、PDF 简历解析、云端朋友包和 HR 回复草稿接口。
- HR 回复助手的基础安全链路已验收：会话不一致会阻止填入；用户已经回复时不重复生成；有效场景只填草稿，不自动发送、不自动同意附件。
- 阶段 A 遗留的“部分 BOSS 页面找不到立即沟通按钮”已明确暂缓，不是阶段 B 当前阻断项。

### 阶段 B

- B1 至 B4 的代码实现、自动化测试和独立审核已经通过：未读发现与去重、后台调度、徽标和队列恢复、会话定位与安全填稿均已落地。
- B5 已完成部分真实页面验收：3 条未读消息归并为 2 个会话；重复检查不重复入队；页面刷新后队列仍为 2；用户最后发言时不会重复填稿。
- C0 修复前的朋友包已能在真实 BOSS 消息列表发现 2 个待回复会话；C0 源码修复后尚未重新打包，因此朋友包不代表当前源码。
- 阶段 B 已按 Beta 口径收口，但当前仍是工作区内的未提交版本，尚未打包、部署或提交。

## 3. 当前阶段 B 收口结论

- 阶段 B 按 Beta 口径收口：真实 BOSS 页面已验证插件加载、真实未读入队、按会话去重、重复扫描不重复入队、页面刷新后队列恢复、用户已回复不重复草稿，以及不自动发送的安全底线。
- 阶段 B 的 C0 紧急源码修复已完成：点击待回复卡片后，若详情页没有暴露 `data-key` 或 HR 文本从列表摘要变成详情全文，插件会退回到严格身份和消息摘要匹配；明确不同的 `data-key` 仍然会拒绝。这不是阶段 C 已启动。
- 修复已通过自动测试，并在真实 BOSS 页面上的开发测试队列成功填入草稿；发送仍需用户手动确认。但用户随后清空了真实未读队列，因此“正常真实未读队列 -> 正确会话 -> 不再误拒”的同场景回归尚未证明通过。
- 旧状态清理源码和自动测试已补齐：历史 `conversation_mismatch` / “当前会话与待回复记录不一致” 的 `needs_user` 项不会再永久压住新扫描；同一会话成功填稿后会清理旧误拒和开发测试重复项。用户手动执行“清空本地记录”已证明旧记录可消失，但没有单独证明自动迁移路径的真实页面效果。
- 某真实会话中的城市类问题返回 `fallback`、`should_fill=false` 是安全护栏的预期结果，不是后端连接失败，也不是当前待修问题。

## 4. 已确认的事实

- BOSS 消息页刚打开时可以只有左侧会话列表，右侧详情为空；未读发现应直接读取左侧列表，不要求先打开聊天详情。
- 当前真实页面有 40 个会话卡片，其中 2 个会话带未读徽标；插件已能识别并归并为 2 个队列项。
- 同一会话可能有多条未读消息，队列数量应按会话去重，而不是按红点数字累加。
- BOSS 切换聊天会话时 URL 可能不变，不能只靠 URL 判断当前会话。
- PDF 解析、快速投递、智能投递和 HR 回复共用一个插件；朋友无需在本地运行 Python 后端。
- 云端 `/match`、`/resume/parse`、`/chat/reply` 路由已存在，`/match` 默认走 fast，聊天接口有独立并发门控。
- 当前误判发生在插件的会话证据校验层，不需要修改或重新部署后端。
- 调试面板和 request ID 已能帮助区分 DOM 识别、会话定位、后端请求和安全兜底问题。

## 5. 未完成的人工验收

- B5-5：海投正在运行时出现真实未读消息，只加入队列和提醒，不抢岗位页、不跳消息页、不打断海投。
- 强制终止并重启 Manifest V3 service worker 后，待回复队列和徽标是否恢复；目前只验证过页面刷新恢复。
- C0 误拒修复的正常真实未读回归：下一条真实 HR 未读出现后，验证正确会话可处理、错误会话仍被阻止、列表摘要变化不误伤正确会话。当前开发测试路径通过，正常未读队列路径未证明通过。
- “找不到立即沟通按钮”的 BOSS 页面兼容问题继续暂缓，等阶段 B 完成后再单独验收。

## 6. 活跃分支窗口及各自职责

| 窗口 | 任务 ID | 职责 | 当前状态 |
| --- | --- | --- | --- |
| B1 | `019fc720-bc78-7811-9a3a-769a0f22bec5` | 未读 DOM 证据、会话指纹、去重和队列迁移 | 已实现，待命排查发现问题 |
| B2 | `019fc710-f63d-7643-a20d-13482f345d3a` | MV3 alarms、退避、消息页扫描和海投任务锁隔离 | 已实现，待命排查调度问题 |
| B3 | `019fc711-0c59-7950-999a-9e92e3bbfa5c` | 徽标、队列状态、缓存迁移和 service worker 恢复 | 已实现，待命排查状态问题 |
| B4 | `019fc0f5-1a19-7231-b9d2-d642ed59f6c2` | 会话复核、任务锁、`/chat/reply`、只填草稿和安全护栏 | C0 误拒源码修复已由主窗口小补丁完成 |
| B5 | `019fc711-1a3d-74b0-a24e-24fb4c431c37` | 真实 BOSS 验收、问题路由、朋友包验证和阶段记录 | Beta 收口；C0 正常真实未读回归待下一事件 |
| 固定审核 | `019fc2ca-8f14-7ef2-bcfb-6bde1a457231` | 只读审核 B 阶段改动，不直接实现 | C0 小补丁后可再做只读审核 |
| 部署 | `019fc288-7fac-7972-8700-15ae995b0dbc` | 仅在后端确有改动且用户明确授权时部署 | 当前不需要启动 |

主窗口只负责产品方向、任务路由、最终验收、记忆和交接，不承接长时间业务实现。

## 7. 本地、云端、朋友包目前分别是什么版本

| 位置 | 当前版本 | 说明 |
| --- | --- | --- |
| 本地源码 | 插件 `0.2.3`；分支 `codex/stage-b-reply-discovery`；基线提交 `f859994` | 阶段 B 改动尚未提交；`plugin/config.js` 默认连接本地后端 |
| 云端后端 | 2026-08-03 已部署的 `server.py` 和 `pipeline.py` | 健康检查为 200；三个业务路由存在；当前会话误判不是云端后端问题 |
| 朋友包 | 插件 `0.2.3`；最后已知构建时间 2026-08-04 12:11 | 该包早于后续 C0 会话误拒与旧状态清理修复，已不与当前本地源码一致；未经用户授权不要重打包 |

朋友包中的令牌不得写入 Git、文档或公开截图；本地源码包和朋友包不能同时启用，避免同一页面出现两个插件实例。

## 8. 当前未提交文件和 Git 状态

- 当前分支：`codex/stage-b-reply-discovery`。
- 当前 HEAD：`f859994`，`main` 目前也指向该阶段 A 提交。
- 已修改但未提交：
  - `.gitignore`
  - `docs/project_memory.md`
  - `plugin/background.js`
  - `plugin/content.js`
  - `plugin/manifest.json`
  - `plugin/popup.html`
  - `plugin/popup.js`
  - `tests/eval_summary.json`（状态显示修改，尚未确认是否存在语义变化）
- 未跟踪：
  - `docs/stage_b_plan.md`
  - `docs/main_handoff.md`
  - `plugin/hr_reply_badge.js`
  - `plugin/hr_reply_discovery.js`
  - `plugin/hr_reply_scheduler.js`
  - `tests/test_hr_reply_background_scheduler.js`
  - `tests/test_hr_reply_badge.js`
  - `tests/test_hr_reply_discovery.js`
  - `tests/test_hr_reply_safety_guards.js`
  - `tests/test_hr_reply_scheduler.js`
- 本轮另新增未跟踪的业务流程图产物：`diagrams/`，包含 Mermaid、SVG、PNG 和 Excalidraw，不属于插件运行时。
- 当前阶段 B 的 tracked diff 为 7 个文件、947 行新增、119 行删除；未跟踪模块、测试、文档和流程图不包含在该统计中。
- 当前没有暂存、提交、部署或重新打包动作。

最新固定回归（2026-08-05）：discovery 64、safety guards 12、badge 10、scheduler 8、background scheduler 8，合计 102 个场景通过；四个插件脚本语法检查和 `git diff --check` 通过，仅有换行符提示。

## 9. 下一步唯一建议

不要继续扩展阶段 B，也不要启动阶段 C。下一步是固定审核和提交准备：只读复核 B/C0 的 diff、测试、文档一致性和敏感信息；通过后再由用户明确授权是否提交或重打朋友包。C0 正常真实未读回归继续保持待验收，不得写成已通过。

## 10. 禁止新主窗口重复做的事情

- 不要重做 B1 至 B3 已完成的未读发现、去重、调度、徽标和队列恢复。
- 不要重新设计阶段 A 的评分、开场白、PDF 解析、快速投递或云端接口。
- 不要把真实会话中的安全 fallback 当成故障，也不要为了让它自动填稿而放松高风险护栏。
- 不要修改后端来修复当前会话误判；问题位于 `plugin/content.js` 的会话证据校验。
- 不要在用户明确授权前部署、打包或提交阶段 B；当前收口只更新文档和验收结论。
- 不要自动发送消息、自动同意附件简历、自动交换联系方式或绕过 BOSS 风控。
- 不要把真实聊天正文、简历、令牌、API Key 或未打码截图写入仓库。
- 不要让主窗口再次执行数小时的实现任务；主窗口只做路由、验收、记忆和交接。
